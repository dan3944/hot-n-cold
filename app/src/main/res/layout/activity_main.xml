<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/background"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="idonthaveadomain.hotcold.MainActivity" >

    <Button
        android:id="@+id/btnGo"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="5dp"
        android:height="150dp"
        android:onClick="clickGo"
        android:text="GO"
        android:width="400dp" />

    <TextView
        android:id="@+id/lblRadius"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/btnGo"
        android:layout_alignLeft="@+id/btnGo"
        android:layout_marginBottom="62dp"
        android:layout_marginLeft="23dp"
        android:text="Miles:"
        android:textSize="20sp" />

    <TextView
        android:id="@+id/lblFind"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_above="@+id/lblRadius"
        android:layout_alignLeft="@+id/lblRadius"
        android:layout_marginBottom="35dp"
        android:text="Find:"
        android:textSize="20sp" />

    <Spinner
        android:id="@+id/spnType"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/lblFind"
        android:layout_marginLeft="30dp"
        android:layout_toRightOf="@+id/lblFind" />

    <Spinner
        android:id="@+id/spnRadius"
        android:layout_width="100dp"
        android:layout_height="wrap_content"
        android:layout_alignRight="@+id/spnType"
        android:layout_below="@+id/spnType" />

</RelativeLayout>